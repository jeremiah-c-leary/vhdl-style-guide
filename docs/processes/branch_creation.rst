Branch Creation
---------------

Issues will be resolved using pull requests.
The branch name will be of the form `issue-[0-9]*` where `[0-9]*` is the issue number.

This will make it easy to link the issue in GitHub with the code to address the issue.
